# rfc_to_display_cli

一个用于把 **RFC 字段 / 协议对象 / 报文结构** 映射到 **华为设备查询 CLI** 的技能仓库。

当前包含：
- `skills/rfc-to-huawei-cli/SKILL.md`

主要能力：
- 从 RFC 字段路径判断观察对象层次
- 将对象映射到华为 `display` / `debugging` 命令族
- 区分原始协议值、当前结果值、统计值、配置值
- 提供 OSPF / BGP worked examples

## 内容说明

这个仓库当前以 skill 文档为主，适合作为后续继续补充协议示例的基础仓库。
